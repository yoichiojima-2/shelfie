# Existing articles in this vault

Your vault already contains the following articles in {language}. Use Obsidian-style wikilinks like `[[slug]]` (or `[[slug|Display Text]]` when the rendered text should differ from the slug) when you mention any of these topics in the body — and **especially** in the `## Related Topics` section, which should be a bulleted list of wikilinks.

You may also wikilink to articles that don't yet exist (e.g. `[[some-future-topic]]`). Obsidian renders these as "ghost" links and clicking creates the missing article later. This is how the vault grows.

{articles}
